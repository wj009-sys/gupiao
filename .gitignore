# Python
venv/
__pycache__/
*.pyc
*.pyo

# Node
node_modules/

# Database
*.db
*.db-wal
*.db-shm

# Temp
*.tmp

# Reports (auto-generated daily, keep template only)
reports/日报/*/*.md
reports/日报/*/*.json
reports/日报/*/*.docx
data/raw/

# Secrets (contain tokens/credentials)
.claude/settings.local.json

# cc-connect 会话数据（自动生成）
.cc-connect/

# cc-connect 配置文件（含Token + 锁文件）
cc-connect.toml
.cc-connect.toml.lock

# 微信扫码二维码（临时生成，不需存档）
wechat_qr_*.png
data/checkpoints/

# 定时任务锁文件（自动生成）
.claude/scheduled_tasks.lock

# 工作树隔离目录（自动生成）
.claude/worktrees/

# 交易日历缓存（自动生成，如不再需版本控制：git rm --cached data/trading_calendar.json）
# data/trading_calendar.json 目前仍被 git 跟踪，完全忽略前需 git rm --cached

# WebUI运行日志
logs/*.log
