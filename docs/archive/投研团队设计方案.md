# A股自动化投研团队 — 完整设计方案

> 基于 `构想.md` 的4个Agent角色，完善职责边界、协作流程、所需资源

---

## 一、整体架构

```
┌──────────────────────────────────────────────────────┐
│                   CLAUDE CODE 主控                      │
│          (编排调度 / /loop / Workflow)                   │
└──────────┬──────────┬──────────┬──────────┬──────────┘
           │          │          │          │
     ┌─────▼──┐ ┌─────▼──┐ ┌───▼────┐ ┌──▼─────┐
     │情报员   │ │分析师   │ │风控官   │ │复盘师   │
     │(Agent1)│ │(Agent2)│ │(Agent3)│ │(Agent4)│
     └────┬───┘ └────┬───┘ └───┬────┘ └───┬─────┘
          │          │          │          │
     ┌────▼──────────▼──────────▼──────────▼───┐
     │           共享基础设施层                   │
     │  memory/ │ data/ │ reports/ │ knowledge/ │
     └──────────────────────────────────────────┘
```

### 协作流程

```
07:00 ──► Agent1 情报员 ──► 情报摘要 (reports/日报/情报)
            │
08:30 ──► Agent2 分析师 ──► 分析报告 (reports/日报/分析)
            │
  盘中 ──► Agent3 风控官 ──► 风控提醒 (按需触发 / cron)
            │
21:00 ──► Agent4 复盘师 ──► 复盘报告 + 更新 knowledge/
```

---

## 二、Agent 职责细化

### Agent1：情报员（信息采集）

**核心职责：**
1. 每天早上7点（cron），自动抓取全网财经资讯
2. 分类整理：宏观政策 / 行业政策 / 公司公告 / 龙虎榜 / 资金流向
3. 输出精炼的情报摘要（Markdown）

**数据来源与对应工具：**

| 信息类型 | 数据来源 | 工具/API |
|---------|---------|---------|
| 宏观/行业政策 | 财联社、华尔街见闻、东方财富 | WebFetch / Tavily Search |
| 公司公告 | 巨潮资讯、东方财富公告 | WebFetch |
| 龙虎榜 | 东方财富龙虎榜、Tushare | tushare-data skill |
| 资金流向 | Tushare资金流向、同花顺 | tushare-data skill |
| 大盘概览 | 新浪财经、腾讯财经 | WebFetch |

**输出格式示例：**
```markdown
# 情报摘要 — 2026-06-26

## 📊 大盘
- 上证综指：xxxx（+0.xx%）
- ...

## 📰 宏观政策
- ...

## 🏭 行业热点
- ...

## 📋 重点公告
- ...

## 💰 资金流向
- 北向资金：净流入 xx 亿
- ...
```

---

### Agent2：分析师（数据处理）

**核心职责：**
1. 拿到情报员的情报后，调用 Tushare 拉取实时行情
2. 计算技术指标：MACD、KDJ、RSI、布林带
3. 对比个股和板块的强度排名
4. 输出分析报告，标记异动板块

**⚠️ 关键依赖：Python 环境**
- Tushare 是 Python 库（`pip install tushare`）
- 技术指标计算需要 `pandas`、`numpy`、`ta-lib` 或 `ta`（Technical Analysis Library）

**两个方案：**

**方案A（推荐）：安装 Python + Tushare**
```
# 需要安装
python >= 3.9
pip install tushare pandas numpy ta matplotlib mplfinance openpyxl
```

**方案B（纯Node.js，功能受限）：**
- 用 `node-fetch` 调新浪/腾讯免费接口获取行情
- 手动实现技术指标计算（或使用 `technicalindicators` npm包）
- 但无法获取完整的 A 股数据（龙虎榜、资金流向等受限）

> **建议采用方案A**，因为 Tushare 是 A 股数据最全的免费接口。

**输出格式：**
```markdown
# 分析报告 — 2026-06-26

## 📈 大盘技术面
- 上证：MACD金叉/死叉、RSI(14)=xx...
- 创业板：...

## 🏆 板块强度排名
1. 半导体 +3.2%
2. 新能源 +2.1%
...

## ⚡ 异动提醒
- xx板块放量突破，MACD金叉
- ...
```

---

### Agent3：风控官（风险管理）

**核心职责：**
1. 大盘环境评估（大盘是否在均线上方？量能是否充足？）
2. 止损线检查（持仓个股是否触发止损？）
3. 仓位管理（当前仓位 vs 设定上限）
4. 盘中实时监控（按需触发）

**需要的数据：**
- 大盘指数实时数据（Tushare / WebFetch）
- 持仓信息（来自本地 `data/portfolio.json`）
- 止损策略（来自 `knowledge/止损规则.md`）
- 仓位规则（来自 `knowledge/仓位管理规则.md`）

**数据来源：**
- 大盘数据：Tushare 大盘指数行情
- 持仓数据：用户手动维护的 `data/portfolio.json`
- 风控规则：用户定义的规则文件

**风控规则示例（用户需自己定义）：**

```yaml
# 大盘规则
大盘跌破20日均线 → 减仓至5成以下
大盘跌破60日均线 → 清仓

# 个股止损
单票亏损达-7% → 强制止损
...

# 仓位管理
单票最大仓位: 20%
总仓位上限: 80%（震荡市）/ 满仓（牛市）
```

---

### Agent4：复盘师（自我进化）

**核心职责：**
1. 每天晚上9点（cron），自动复盘当天研判
2. 对比盘前预测 vs 实际走势，计算准确率
3. 分析偏差原因（市场环境变化？指标失效？信息遗漏？）
4. 更新知识库/策略库

**需要的数据：**
- 当天的情报摘要（Agent1的输出）
- 当天的分析报告（Agent2的输出）
- 当天实际行情走势（Tushare）
- 以往复盘记录（`knowledge/复盘记录/`）

**输出格式：**
```markdown
# 复盘报告 — 2026-06-26

## ✅ 预测总结
- 看涨板块：半导体→实际+2.1% ✅
- 看空板块：地产→实际-0.3% ✅
- 准确率：xx%

## 🔍 偏差分析
- 失误1：xx（原因：政策突变，信息未及时捕捉）
- 失误2：xx（原因：MACD金叉信号滞后）

## 📝 策略优化建议
- ...
```

---

## 三、目录结构设计

```
C:\Users\65004\Desktop\小白\股票投资\
│
├── 构想.md                          # 原始想法
├── 投研团队设计方案.md                # 本文件（设计方案）
├── CLAUDE.md                        # 项目配置（需要创建）
│
├── memory/                          # Claude记忆系统（已有）
│   ├── 投研-项目目标.md
│   ├── 投研-持仓信息.md
│   └── ...
│
├── data/                            # 数据存储
│   ├── portfolio.json               # 持仓信息
│   ├── watchlist.json               # 自选股列表
│   ├── 仓位管理规则.json              # 仓位规则
│   ├── 止损规则.json                  # 止损规则
│   └── 策略规则.json                  # 交易策略
│
├── reports/                         # 报告输出
│   ├── 日报/
│   │   ├── 情报/
│   │   │   └── 情报摘要_2026-06-26.md
│   │   ├── 分析/
│   │   │   └── 分析报告_2026-06-26.md
│   │   └── 复盘/
│   │       └── 复盘报告_2026-06-26.md
│   └── 周报/
│
├── scripts/                         # 脚本代码
│   ├── agent1-情报采集/              # Agent1 脚本
│   │   └── fetch_news.py
│   ├── agent2-技术分析/              # Agent2 脚本
│   │   ├── macd.py
│   │   ├── kdj.py
│   │   └── sector_ranking.py
│   ├── agent3-风控/                  # Agent3 脚本
│   │   └── risk_check.py
│   ├── agent4-复盘/                  # Agent4 脚本
│   │   └── review.py
│   └── utils/                        # 工具函数
│       ├── tushare_client.py
│       └── report_formatter.py
│
├── skills/                           # 自定义Skills（可选）
│   └── ...
│
└── knowledge/                        # 知识库（Agent4 维护）
    ├── 策略/
    │   ├── 选股策略.md
    │   └── 择时策略.md
    ├── 复盘记录/
    │   └── 复盘_2026-06-26.md
    ├── 止损规则.md
    ├── 仓位管理规则.md
    └── 准确率统计.md
```

---

## 四、需要安装的工具

### 1. Python 环境（必须，Tushare依赖）
```bash
# 推荐安装 Miniconda 或 直接装 Python
# 然后在项目目录创建虚拟环境
python -m venv venv
venv\Scripts\activate

# 安装依赖
pip install tushare pandas numpy ta matplotlib mplfinance
# ta = Technical Analysis Library（比TA-Lib好装，纯Python）
# 如需更精确指标可尝试安装 TA-Lib（需C库，Windows较麻烦）
```

### 2. Tushare API Token（免费注册）
- 注册地址：https://tushare.pro
- 注册后获取 token，配置到环境变量或脚本中

---

## 五、可用的 Skills 清单

以下是当前已安装/可用的、与你项目相关的 Skills：

| Skill 名称 | 用途 | 适用Agent |
|-----------|------|-----------|
| **tushare-data** | 直接通过Claude调用Tushare获取A股数据（行情、财务、龙虎榜等） | Agent1, Agent2, Agent3 |
| **tavily-search** | AI原生搜索引擎，抓取最新财经资讯、新闻 | Agent1 |
| **portfolio-monitor** | 组合监控，可能已有持仓监控逻辑 | Agent3 |
| **xlsx** | 读写Excel报表 | Agent2（输出报告） |
| **deep-research** | 深度研究，多源验证，适合调研行业/公司 | Agent1, Agent4 |
| **feynman** | 费曼思维模型，适合复盘时的因果分析 | Agent4 |
| **jesse-livermore-perspective** | 利弗莫尔视角（趋势交易思想） | Agent2, Agent3（辅助决策） |
| **ray-dalio-perspective** | 达利欧视角（周期、风险平价） | Agent3, Agent4 |
| **taleb** | 塔勒布视角（黑天鹅、反脆弱） | Agent3 |
| **zhang-yiming** | 张一鸣视角（信息效率、认知迭代） | Agent4 |
| **karpathy** | Karpathy视角（系统化思维） | Agent2 |
| **ilya-sutskever** | Ilya视角（深度思考、第一性原理） | Agent4 |
| **skill-creator** | 创建自定义Skill | 用于将Agent打包为Skill |
| **update-config** | 配置settings.json | 设置cron、权限、hooks |

---

## 六、MCP 服务器建议

MCP 是 Claude Code 扩展能力的标准方式。以下是推荐配置：

### 🔧 必须配置

| MCP | 用途 | 针对Agent |
|----|------|-----------|
| **Tushare MCP**（自制） | A股行情、财务、龙虎榜、资金流向 | Agent1, Agent2 |
| **WebFetch**（自带） | 抓取财联社、东方财富等网页 | Agent1 |

### 🔧 推荐配置

| MCP | 用途 |
|----|------|
| **Playwright MCP** | 如需无头浏览器抓取动态页面（东方财富、同花顺有反爬） |
| **GitHub MCP**（市场已有） | 如需备份策略脚本到GitHub |
| **Telegram MCP**（市场已有） | 推送报告/预警到手机通知 |
| **Discord MCP**（市场已有） | 团队协作/推送通知 |

> 注：市场已有的 MCP（GitHub/GitLab/Telegram/Discord）在 `external_plugins/` 目录下，需通过 `/mcp-builder` skill 或手动配置安装激活。

### 🛠️ 需要自制的 MCP（Tushare）

Tushare 目前是 Python skill 形式。要变成独立的 MCP 服务器，需要：
1. 用 Node.js 或 Python 写一个 MCP server
2. 封装 Tushare 的关键API（行情、日线、资金流向、龙虎榜等）
3. 通过 `stdio` 或 `http` 暴露给 Claude

或者更简单：直接在 CLAUDE.md 中写好 Tushare skill 的调用方式即可，不用单独建 MCP。

---

## 七、CLAUDE.md 项目配置

这是项目根目录的配置文件，告诉 Claude 这个项目的上下文。需要创建 `CLAUDE.md` 文件：

```markdown
# 股票投研自动化项目

## 项目目标
构建A股自动化投研团队，包含4个AI Agent角色

## Agent 团队
- Agent1 情报员：早7点采集财经资讯
- Agent2 分析师：技术分析、板块排名
- Agent3 风控官：大盘评估、止损、仓位管理
- Agent4 复盘师：晚9点复盘、计算准确率、更新知识库

## 目录结构
- data/ - 数据文件（持仓、自选、规则）
- reports/ - 报告输出
- scripts/ - Python脚本
- knowledge/ - 知识库（由Agent4维护）
- memory/ - Claude记忆

## 数据源
- Tushare API (token在环境变量 TUSHARE_TOKEN)
- 网页抓取：财联社、东方财富、巨潮资讯

## 规则
- 止损线：单票 -7%
- 仓位上限：单票20%，总仓位80%
- 技术指标：MACD(12,26,9), KDJ(9,3,3), RSI(14), BOLL(20)

## 定时任务
- 07:00 - Agent1 情报采集
- 08:30 - Agent2 技术分析
- 21:00 - Agent4 复盘
```

---

## 八、实施路线图

### Phase 1：基础搭建（1-2天）
1. ✅ 安装 Python + 虚拟环境 + pip 依赖
2. ✅ 注册 Tushare 并获取 Token
3. ✅ 创建 CLAUDE.md 项目配置
4. ✅ 创建目录结构（data/ reports/ scripts/ knowledge/）
5. ✅ 配置 `data/portfolio.json` 初始持仓

### Phase 2：Agent 脚本（2-3天）
1. 编写 `scripts/utils/tushare_client.py`（封装 Tushare 调用）
2. 编写 Agent1 情报采集脚本
3. 编写 Agent2 技术分析脚本
4. 编写 Agent3 风控检查脚本
5. 编写 Agent4 复盘脚本

### Phase 3：Skill 封装（1天）
- 使用 `skill-creator` 将每个 Agent 封装为独立 Skill
- 或直接在 CLAUDE.md 中定义好 prompt template

### Phase 4：自动化调度（1天）
- 配置 Claude Code 的 cron hooks（通过 settings.json）
  - `0 7 * * 1-5` → Agent1 采集
  - `30 8 * * 1-5` → Agent2 分析
  - `0 21 * * 1-5` → Agent4 复盘
- Agent3（风控）由盘中手动触发或按需调用

### Phase 5：迭代优化（持续）
- 根据复盘结果优化策略
- 扩展数据源
- 添加通知推送（Telegram）

---

## 九、当前资源与实际差距分析

| 需求项 | 当前状态 | 操作 |
|-------|---------|------|
| Node.js | ✅ v24.15 | 无需操作 |
| Python + pip | ❌ 未安装 | 需安装 Python 3.9+ |
| Tushare | ❌ 未注册/未装 | 注册tushare.pro + pip install |
| pandas/numpy/ta | ❌ 未装 | pip install |
| memory/ 目录 | ✅ 已存在（空） | 写入记忆 |
| CLAUDE.md | ❌ 未创建 | 创建 |
| data/ 目录 | ❌ 未创建 | mkdir |
| reports/ 目录 | ❌ 未创建 | mkdir |
| scripts/ 目录 | ❌ 未创建 | mkdir |
| knowledge/ 目录 | ❌ 未创建 | mkdir |
| Telegram 通知 | ❌ 未配置 | 可选，非必须 |

---

## 十、下一步建议

1. **先装 Python** — 一切的基础。推荐装 [Python 3.11+](https://www.python.org/downloads/) 并勾选"Add to PATH"
2. **注册 Tushare** — 去 tushare.pro 注册，拿 token
3. **让我帮你一次性创建好** — 目录结构、CLAUDE.md、初始 data 文件、记忆文件
4. **分阶段实现** — 先做 Agent1（情报员），跑通后再逐步增加

要现在开始动手吗？
